# Codex Review：内容补充建议

- **评审来源：Codex 对本仓库的内容与维护流程 review。**
- 评审日期：2026-09-05。
- 评审范围：README、24 个 YAML 分类、生成脚本、贡献规范与 CI，以及部分官方参考资料。
- 状态：建议清单，供维护者排期；不代表已采纳、已实现或已经完成全部规格核验。

## 评审结论

仓库已有较广的硬件覆盖，下一步最有价值的是补齐从规格到部署选择的信息：具体模型需要多少资源、如何比较性能、如何选择多卡和软件组合。

延续现有定位：用可核验的数字、简洁的对照表和容量计算帮助选型。原理性长文继续放在姊妹项目 [qfc-network/ai-infra](https://github.com/qfc-network/ai-infra)。

## 优先级与建议交付物

以下文件名均为建议，尚未创建。P0 为建议首批内容，P1 为后续扩展，P2 为较高维护成本的补充。

| 优先级 | 内容 | 建议数据文件 | 解决的问题 |
|---|---|---|---|
| P0 | 具体模型容量实例 | `data/model-sizing.yaml` | 我的模型需要多少显存？ |
| P0 | 推理性能与测试口径 | `data/inference-metrics.yaml` | 两个跑分能否直接比较？ |
| P0 | 数据中心 PCIe 推理卡 | `data/pcie-gpus.yaml` | SXM / DGX 之外有哪些部署选择？ |
| P1 | 多卡部署方式 | `data/parallelism.yaml` | 模型和请求应该怎样分到多张卡？ |
| P1 | 微调显存预算 | `data/finetuning-memory.yaml` | 能推理的模型是否也能微调？ |
| P1 | 软件兼容组合 | `data/validated-configs.yaml` | 哪个硬件、系统和引擎组合经过验证？ |
| P2 | 云端加速器与实例映射 | `data/cloud-accelerators.yaml` | 租到的实例实际提供什么资源？ |

## P0：建议先补的三块内容

### 1. 具体模型容量实例

现有量化表和 KV cache 表提供通用公式，但缺少把两者合起来的完整案例。

- 首批选择有公开配置的 Dense 与 MoE 模型，固定模型 ID、revision 和配置来源。
- 列出总参数、激活参数、权重量化格式、KV 类型、上下文长度、并发数、权重占用、KV 占用和运行时预留。
- 同一模型提供 8K / 32K 上下文、单请求 / 多请求的对照。
- 明确估算与实测；MoE 的激活参数不能直接作为全部常驻权重的容量依据。
- 常规 MHA / GQA 公式与 MLA、滑动窗口、混合架构分别标注适用条件。

**验收标准：**读者能够根据来源、公式和假设复算每行结果；“能装下”与实际速度、可接受的服务延迟分别说明。

### 2. 推理性能与测试口径

增加指标表与可复现的测试记录模板：

- 指标包括首 token 延迟（TTFT）、每输出 token 时间（TPOT）、token 间延迟（ITL）、端到端延迟、单请求速度和服务总吞吐。
- 记录 P50 / P95、输入输出长度、并发或请求到达率、预热方式、缓存状态、错误率，以及模型、引擎和驱动版本。
- 分开描述 prefill 与 decode；注明是否启用前缀缓存、推测解码等优化。
- 固定测试条件后再比较硬件，不把峰值算力或显存带宽倍率直接当成真实出词速度倍率。

可参考 [vLLM Benchmark CLI](https://docs.vllm.ai/en/latest/benchmarking/cli/)的指标定义与命令。落地时固定具体版本，避免文档和命令漂移。

**验收标准：**提供至少一套可执行的测试命令和结果记录格式；没有实际运行的结果明确留空，不填推测跑分。

### 3. 数据中心 PCIe 推理卡

目前 NVIDIA 数据中心内容以 SXM、DGX 和机柜为主。建议补充 T4、A10、L4、L40S、H100 PCIe 等候选条目。

- 字段包括显存、带宽、功耗、形态、PCIe 代际、卡间互联、原生精度、ECC / MIG 支持及适用工作负载。
- 同名芯片的 PCIe、SXM、NVL 等配置分别核验，不复用不同形态的规格。
- 用官方数据补出低功耗和通用服务器部署档位；例如 [NVIDIA L4 产品页](https://www.nvidia.com/en-us/data-center/l4/)可作为起始来源。

**验收标准：**每个条目有对应形态的官方来源，算力统一标明精度、稀疏性和单卡口径。

## P1：连接容量、互联和软件

### 4. 多卡部署方式对照

整理 TP、PP、DP、EP：切分对象、权重是否复制、KV cache 分布、通信模式、互联需求及适用场景。说明训练与推理中术语和实现的差异。

提供“单卡装得下”“单卡装不下但单机装得下”“必须跨节点”三个案例，关联现有互联与推理引擎表。

**验收标准：**案例写明使用的引擎与版本；建议基于实际拓扑，避免把“没有 NVLink”直接等同于“不能多卡”。

### 5. 微调显存预算

对比全参数微调、LoRA、QLoRA，拆分权重、梯度、优化器状态、激活和临时缓冲区。补充序列长度、micro-batch、梯度检查点、ZeRO / FSDP 和卸载的影响。

可参考 [Hugging Face Model training anatomy](https://huggingface.co/docs/transformers/v4.48.0/model_memory_anatomy)的内存组成说明；具体预算还需绑定训练配置。

**验收标准：**至少提供一个完整配置实例；不把某种优化器或混合精度配置下的每参数字节数当成通用常数。

### 6. 软件兼容组合表

把现有计算栈和引擎概览连接为：硬件 → OS → 驱动 / runtime → 框架 / 引擎版本 → 量化 kernel → 模型。

- 区分官方声明支持、社区支持和本仓库实测。
- 记录验证日期、来源、启动命令及已知限制。
- 首批只维护少量代表性组合，避免承诺全量兼容矩阵。

**验收标准：**每条“已实测”都有可复现记录；只有文档依据的条目不能标成实测。

## P2：云端资源补充

### 7. 云端加速器与实例映射

补充 Google TPU、AWS Trainium / Inferentia，以及云实例对应的加速器型号、数量、单芯片内存、互联、主机资源和软件入口。TPU 可从 [官方架构文档](https://docs.cloud.google.com/tpu/docs/system-architecture-tpu-vm)开始整理。

**验收标准：**区分芯片、实例和集群规格；可用区域和价格若收录，必须附日期、计费方式与来源，作为独立快照维护。

## 扩充前应处理的现有问题

以下是 Codex 在本次 review 中发现的仓库内部问题，尚未在本次文档变更中修复。

| 问题 | 现有位置 | 建议处理 |
|---|---|---|
| FP4 支持范围相互矛盾 | [quantization.yaml](data/quantization.yaml) 合并 NVFP4 / MXFP4 后写“仅 Blackwell”，但 [amd-gpus.yaml](data/amd-gpus.yaml)列出了 MI350X / MI355X 的 MXFP4 | 拆分格式与硬件支持，分别核验 |
| 本地容量概括与条目冲突 | [local-options.yaml](data/local-options.yaml)称 48 GB 以上都是统一内存，同表又包含 96 GB 独立显存卡 | 改为明确区分独立显存和统一内存的描述 |
| 来源规则缺少检查 | [CONTRIBUTING.md](CONTRIBUTING.md)要求条目附来源，但 [generate.py](scripts/generate.py)未检查 `sources`，部分条目也未填写 | 为规格、推导和跨表汇总分别定义来源或引用要求，再加入校验 |
| 核验时效不明确 | 尚无统一的核验日期与产品状态元数据 | 设计 `last_verified`、产品状态及证据类型；若要显示在文档中，同步扩展生成器 |

## 建议实施顺序与完成条件

1. 修复两处表间矛盾，明确来源与日期的记录约定。
2. 分别提交模型容量实例、性能测试口径、PCIe 推理卡三个独立变更。
3. 再补多卡、微调和兼容组合；云资源部分根据维护能力排期。

新增表格沿用现有流程：修改 `data/`、生成器的 `ORDER` 和两份 README 模板，再生成中英双语输出。测试命令、原始测量记录等辅助资料可单独组织，不把完整日志塞进表格。

```bash
python scripts/generate.py
python scripts/generate.py --check
```

每个内容变更需检查来源、计算假设、双语一致性和目录链接。生成检查通过仅代表结构与输出一致，不代表规格或跑分已经核实。

本文件是手工维护的 **Codex Review 记录**，放在仓库根目录，避免与 `docs/` 下的自动生成文档混淆。
